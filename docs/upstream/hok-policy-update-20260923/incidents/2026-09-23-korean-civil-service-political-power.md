# 2026-09-23 공직 감찰 정비의 매일 정치력 강화

사용자가 제시한 공직 감찰 정비 화면의 매일 정치력 **+0.25를 최종 +1.0으로 변경**했다. 추가 +1.0이나 퍼센트 보너스가 아니라 고정 일일 정치력 +1.0이다.

## 기준선과 적용

- 브랜치 `feat/korean-focus-expansion-ai`, HEAD `be5fb40dbd8de33e5adbf65e808bcb0e8c283560`. 앞선 디시전 7종 비용 변경의 미커밋 작업을 보존했다.
- 설치본은 HOI4 `1.19.3.0.c01a (5632)`다. 런처 `mod/hearts of korea.mod`는 작업 저장소를 직접 가리킨다. 저장소·런처의 `supported_version="1.19.*"`, 선언 의존성 `Korean Language`, 후속 Workshop ID `3793992662`는 일치하며 `replace_path`는 없다.
- 실행 목록은 로컬 모드 하나, 비활성 DLC 목록은 비어 있다. 선언 의존성은 활성 목록에 없으며 이번에는 DLC·체크섬·게임 화면을 새 실행으로 검증하지 않았다.
- `HOK_KOR_civil_service_audit`는 `HOK_KOR_accountable_administration_idea`를 지급한다. `common/ideas/HOK_KOR_democratic_expansion.txt`의 해당 `political_power_gain`을 `0.25`에서 `1.0`으로 변경했다.
- 국회 결산위원회가 하위를 제거하고 부여하는 `HOK_KOR_public_accounts_idea`도 `1.0`을 승계한다. 상위의 안정도 +2%, 중점 완료 즉시 안정도 +10%p, 민주정부 유지 조건과 기존 교체 구조를 보존했다. 두 단계는 중첩되지 않는다.
- 같은 디렉터리·modifier 문맥의 설치본 `common/ideas/GER.txt:61`, `japan.txt:172`에서 고정 `political_power_gain = 1` 사례를 확인했다.

한국어·영어 채널의 공개 결산 제도 설명과 현행 60개 명세의 두 행을 +1.0에 맞췄다. 과거 재조정 설계의 당시 수치는 보존하고 후속 변경 안내를 추가했다. 중점 자체·배치·AI·이미지·다른 보너스는 변경하지 않았다.

## 검증

독립 검토에서 작업 직전 스냅샷과 대조해 두 정치력 수치·기여자 주석, 설명 수치와 문서 기록 외에 변경이 없음을 확인했다. 중점 파일 해시는 동일하며 국민정신 ID·민주정부 취소·교체 구조와 상위 안정도는 유지된다. 따라서 이 검토는 보상 값과 기존 연결의 소스 확인이며 실제 엔진 지급 검증은 아니다.

현지화 두 파일의 UTF-8 BOM·83개 키·키 순서·중복 없음·따옴표 구조·동일 본문을 확인했다. 수정 대상 5개 파일의 기존 BOM 상태를 보존했고 `git diff --check`를 통과했다. 앞선 작업과 겹치지 않는 11개 파일의 해시가 동일하며, 이번에 함께 수정한 현지화·문서에도 앞선 정치력 150·민공 미점유 변경이 남아 있다.

실행 중인 사용자 게임을 확인했으므로 종료·재시작·재로드를 수행하지 않았다. 직전 작업에서 확인한 PowerShell 검증 스크립트 실행 정책 차단도 해제하거나 우회하지 않았다. 이번 변경의 새 게임 로드·실제 매일 정치력 지급·상위 교체 UI·기존 저장·DLC·멀티플레이 검증은 미실시다.

작업 직전 관련 파일과 이전 작업의 해시, 정적 검토 보고서는 Git 제외 경로 `.local-artifacts/civil-service-pp-20260923/`에 보존했다. 커밋·푸시·업로드는 수행하지 않았다.
